{
  "name": "the-ledger",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check-gates.mjs && node scripts/check-training.mjs",
    "test": "node --test \"tests/*.test.mjs\" && node scripts/test-solutions.mjs"
  },
  "dependencies": { "three": "^0.186.0", "vue": "^3.4.21" },
  "devDependencies": { "@vitejs/plugin-vue": "^5.0.4", "vite": "^5.2.0" }
}
