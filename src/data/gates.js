// Gate content. One object per gate; see CLAUDE.md for the voice and schema.
const g = (id, rank, xp, stat, title, algo, story, mission, hint, code) => ({ id, rank, xp, stat, title, algo, story, mission, hint, code });

const ARCS = [
{ name:'Arc I — Recruitment', sub:'Marguerite is watching. Prove you can hold a list.', gates:[
  g('fizz','F',40,'logic','Radio check','Loops & conditionals',
    ['A burner phone buzzes on the laundromat counter. A woman\'s voice, no greeting. This is Marguerite.',
     '“Crew radio protocol. We count up. Every third count you say clear, every fifth you say hold, both at once you say clear hold. Otherwise you say the number.”',
     '“Write it down so the new muscle cannot get it wrong. He will try.”'],
    'Write radio_check(n) returning a list of n strings for the counts 1..n: "clear" for multiples of 3, "hold" for multiples of 5, "clear hold" for both, otherwise the number as a string. Stretch: put the rules in a table so a new word can be added without a new if-branch.',
    'Test the both-case first, or build each string by appending the words that apply and fall back to the number when nothing applied.',
`radio_check(15)
# ['1', '2', 'clear', '4', 'hold', 'clear', '7', '8', 'clear', 'hold',
#  '11', 'clear', '13', '14', 'clear hold']`),
  g('zeros','F',40,'logic','Cleaning the stash','Arrays, in-place writes',
    ['A backroom above a laundromat. Marguerite tips a tray of stolen SIM cards onto the table. Some slots are empty — value 0.',
     '“Pack the real ones to the front, in the order they came. Dead slots to the back. Do it on the tray, I am not giving you a second tray.”'],
    'Write move_zeros(nums) that moves every 0 to the end of the list in place, keeping the order of the other numbers. No new list.',
    'Keep one finger on the next slot that should receive a real card. Walk the tray once with a second finger.',
`nums = [0, 1, 0, 3, 12]
move_zeros(nums)
print(nums)   # [1, 3, 12, 0, 0]`),
  g('transpose','F',40,'speed','The teller floor, sideways','2-D lists & index swaps',
    ['A photo of the teller floor plan, rows of desks, taken from the mezzanine. Marguerite needs it as the guards see it: rotated a quarter turn.',
     'Dax offers to redraw it by hand. Marguerite does not look up.',
     '“Rows become columns. Every desk keeps its number. Do not lose one.”'],
    'Write transpose(grid) returning a new grid where row r, column c of the input lands at row c, column r. It must work for non-square grids (3×2 becomes 2×3). Stretch: rotate a square grid 90° clockwise in place.',
    'The new row number is the old column number. Build each output row by reading one column top to bottom.'),
  g('palin','F',40,'logic','The passphrase','Two pointers on a string',
    ['The fence only answers if the passphrase reads the same in both directions. Punctuation and case do not count, she is not a monster.',
     '“Check it before you knock. Get it wrong and she hears you get it wrong.”'],
    'Write is_palindrome(s) ignoring non-alphanumeric characters and case. One pass, no reversed copy.',
    'One pointer at each end. Skip anything that is not a letter or digit. Compare, step inward.',
`is_palindrome("A man, a plan, a canal: Panama")  # True
is_palindrome("race a car")                        # False`),
  g('reverse','F',40,'speed','Coded messages','String reversal & word swaps',
    ['Dax writes his notes backwards. Every word. Says it keeps the cops honest.',
     '“Translate his last drop. Then reverse the word order, because he also does that.”'],
    'Write reverse_words(s) that reverses the order of words in a sentence and trims extra spaces.',
    'Split, reverse, join is the whole game. Ask yourself what the in-place version would look like with pointers.'),
  g('plusone','F',40,'logic','The next serial','Digit arrays & carries',
    ['Deposit-box serials are stamped one digit per plate. The forger needs the serial after 1-9-9-9 and cannot read numbers, only plates.',
     '“Give me the next serial as plates. No, you may not glue them into a number and add one. That is cheating and he cannot do it anyway.”'],
    'Write plus_one(digits) taking a list of single digits (most significant first) and returning the digits of that number plus one. No conversion to int. Stretch: what happens with [9, 9, 9], and how long is the result?',
    'Start from the rightmost plate. A 9 becomes 0 and carries left. If you run off the front still carrying, you need a new plate.',
`plus_one([1, 2, 3])   # [1, 2, 4]
plus_one([1, 9, 9])   # [2, 0, 0]
plus_one([9, 9])      # [1, 0, 0]`),
  g('twosum','F',50,'memory','The bribe','Hash map lookups',
    ['A guard wants exactly 1,270 in used bills. You have a bag of assorted notes and one chance to hand over two of them.',
     '“Find the pair. And do not compare every note against every other note, I have watched you do that.”'],
    'Write two_sum(nums, target) returning the indices of two numbers that add to the target. One pass.',
    'For each note, ask: have I already seen its partner? A dictionary remembers what you have seen.',
`two_sum([2, 7, 11, 15], 9)   # [0, 1]`),
  g('rmdup','F',50,'speed','Thinning the guest list','Two pointers, in-place writes',
    ['The gala guest list came from three sources and was sorted by badge number. Same badge, three lines.',
     '“One line per badge, in order, on the same sheet. Tell me how many real guests that leaves. Same trick as the SIM tray, if you were paying attention.”'],
    'Write remove_duplicates(nums) for a sorted list: shift the unique values to the front in place and return their count k. The first k entries must be the unique values in order; what sits past k does not matter. Stretch: allow each badge at most twice.',
    'A slow finger marks where the next unique value should be written. A fast finger reads. Write only when the value differs from the last one written.',
`nums = [0, 0, 1, 1, 1, 2, 2, 3, 3, 4]
k = remove_duplicates(nums)
print(k, nums[:k])   # 5 [0, 1, 2, 3, 4]`),
  g('anagram','F',50,'memory','Forged IDs','Frequency counting',
    ['Two ID cards, same letters scrambled. One is real.',
     '“Tell me if these two names are made of the same letters. Then tell me how many fake IDs in the pile share letters with a real one.”'],
    'Write is_anagram(a, b). Then write group_anagrams(words) returning the words grouped into anagram sets (a list of lists, any order).',
    'Count letters. Two words are anagrams if their counts match. A sorted word makes a fine dictionary key.'),
  g('xor','F',50,'logic','The odd key','Bit tricks (XOR)',
    ['A ring of keys from a locker room. Every key was cut in pairs, one for the guard and one for the desk. Except one. That one opens something nobody wants you to open.',
     'Dax: “Sort them. Walk them two at a time.”',
     '“He is right, and it costs us a sort. Do it in one pass and without writing anything down.”'],
    'Write single_number(nums) where every value appears exactly twice except one; return that one in O(n) time and O(1) extra memory. Stretch: write missing_number(nums) for a list holding every number 0..n except one, using the same trick.',
    'A number XOR-ed with itself is 0, and XOR with 0 changes nothing. Pairs cancel; the loner survives.',
`single_number([4, 1, 2, 1, 2])   # 4
missing_number([3, 0, 1])        # 2`),
  g('dupes','F',50,'memory','The double-counted crate','Sets',
    ['Inventory says 412 crates. Dax counted 412. Marguerite counted 409.',
     '“Someone scanned a serial twice. Find it.”'],
    'Write has_duplicate(nums) and find_duplicates(nums). Then explain why sorting first also works, and which is faster.',
    'A set only holds each thing once. Compare its size to the list, or check membership as you go.'),
  g('squares','E',60,'speed','Distances from the vault','Two pointers from both ends',
    ['Sensor positions along the vault corridor, measured from the vault door: negative is west, positive is east, already sorted.',
     'The buyer\'s jammer works on squared distance. Dax squares every number and calls sorted().',
     '“He gets the right answer in n log n. Give it to me in n. The list is already sorted, use that.”'],
    'Write sorted_squares(nums) returning the squares of a sorted list, also sorted, in O(n). Stretch: explain why the largest square always sits at one of the two ends of the input.',
    'The biggest squares live at the ends, not the middle. Fill the answer from the back, comparing the two ends and taking the larger.',
`sorted_squares([-4, -1, 0, 3, 10])   # [0, 1, 9, 16, 100]`),
  g('majority','E',60,'memory','The loudest voice','Counting & vote cancelling',
    ['A leaked list of votes from the branch staff meeting. Someone wanted the night shift schedule changed badly enough to rig it, and rigged it so hard they have more than half the votes.',
     '“Name them. A dictionary of counts first. Then do it again with two variables, because I want to see you understand why it works.”'],
    'Write majority_element(nums) returning the value that appears more than n/2 times. First with a count dictionary, then in O(1) extra memory. Stretch: if the more-than-half guarantee is dropped, how do you verify the candidate?',
    'Keep one candidate and a counter. Same value: count up. Different: count down. At zero, adopt the current value. The majority survives every cancellation.'),
  g('stocks','E',60,'speed','When to sell the diamonds','Single-pass tracking',
    ['A list of the black-market price of the diamonds, one entry per day. You can sell once, but only after you buy once.',
     '“Maximum profit. And no, you may not see the future twice.”'],
    'Write max_profit(prices). One pass, O(1) extra memory.',
    'Walk forward remembering the cheapest day so far. Each day, ask how much you would make selling now.',
`max_profit([7, 1, 5, 3, 6, 4])   # 5`),
  g('firstuniq','E',60,'memory','The key nobody used twice','Frequency counting, two passes',
    ['The badge reader log for the staff door, one character per swipe. Most badges swipe in and out. One swiped once and never again. That is the badge we clone.',
     '“Which swipe was it, first one that never repeats. Position, not the badge. And count once, not once per badge.”'],
    'Write first_unique_char(s) returning the index of the first character that appears exactly once, or -1 if there is none. Stretch: can it be done in a single pass over s with an ordered dictionary?',
    'Count every character in one pass. Walk the string a second time and stop at the first character whose count is 1.'),
  g('parens','E',60,'logic','The bracket lock','Stacks',
    ['The stairwell door takes a code made of brackets. It only opens if every bracket closes properly.',
     '“Validate the code. The door remembers the last three people who got it wrong.”'],
    'Write is_valid(s) for strings of ()[]{}.',
    'Push openers. When you see a closer, the most recent opener must match. What structure gives you the most recent thing?'),
]},
{ name:'Arc II — Casing the bank', sub:'Weeks of watching. Patterns in the noise.', gates:[
  g('halden','E',80,'logic','The vault at Halden Street','Binary search',
    ['Marguerite slides a tablet across the table.',
     '“Halden Bank. The vault door has a keypad rack: one million numbered brass keys, laid out in sorted order along the wall. Key 734,219 opens the door. Turn the wrong one and the alarm is silent, which is worse.”',
     '“The guard walks the corridor every 25 seconds. Reading a key takes 1 second. Turning one takes 3. You get one pass.”',
     'Dax shrugs. “Walk the rack. Read them one by one.”',
     'Marguerite looks at you. “Is he right?”'],
    'Write find_key(rack, target) returning the index of the target with as few reads as possible. Then answer: how many reads does Dax need in the worst case, and how few can you get away with in 22 seconds?',
    'Sorted means every read tells you about half the rack, not one key. Ask what a single read in the middle rules out.',
`import random

rack = sorted(random.sample(range(1, 5_000_000), 1_000_000))
target = rack[734_219]
reads = 0

def read(i):
    global reads
    reads += 1
    return rack[i]

def find_key(rack, target):
    # only look at keys via read(i) — that's the cost
    ...

idx = find_key(rack, target)
print(idx, "found in", reads, "reads")`),
  g('rotate','E',80,'speed','Shifting the shift','Array rotation by reversal',
    ['The guard rota is a list of names, and every Monday the bank rotates it by k places: the last k move to the front.',
     'Dax: “Pop the last one, stick it on the front. Do that k times.”',
     '“That is k passes over the list. I want one, and I want it done on the list itself, not on a copy.”'],
    'Write rotate(nums, k) that rotates the list right by k positions in place with O(1) extra memory. Stretch: handle k larger than the list length without doing extra work.',
    'Reverse the whole list. Then reverse the first k. Then reverse the rest. Draw it once and it stops being magic.',
`nums = [1, 2, 3, 4, 5, 6, 7]
rotate(nums, 3)
print(nums)   # [5, 6, 7, 1, 2, 3, 4]`),
  g('merge','E',80,'logic','Two watch logs','Merging sorted sequences',
    ['Dax and Marguerite each logged guard sightings by timestamp. Two sorted lists.',
     '“One timeline. Do not sort the whole thing again, you already have two sorted halves.”'],
    'Write merge(a, b) combining two sorted lists into one sorted list in O(n + m).',
    'A finger on each list. Take the smaller, advance that finger. When one runs out, dump the other.'),
  g('firstbad','E',80,'speed','When the camera went blind','Binary search on a yes/no boundary',
    ['The lobby camera runs firmware, versions 1 through n. Some version introduced the blind spot. Every version after it has the blind spot too.',
     'Checking one version means Dax climbs the pole with a laptop. He has agreed to climb it a small number of times.',
     '“Find the first bad version. Every climb you save is a climb he does not complain about.”'],
    'Write first_bad_version(n, is_bad) where is_bad(v) is an expensive function that returns True from the first bad version onward. Return that version with as few calls to is_bad as possible. Stretch: how many calls for n = 1,000,000,000?',
    'Good versions form a prefix, bad ones a suffix. Probe the middle: bad means the boundary is at or before it, good means it is after. Same halving as the key rack.',
`climbs = 0
def is_bad(v):
    global climbs
    climbs += 1
    return v >= 1_702

print(first_bad_version(5_000, is_bad), "after", climbs, "climbs")
# 1702 after 13 climbs`),
  g('window','D',90,'speed','The camera blind spot','Sliding window',
    ['A string of camera states across the lobby, one character per second: X for covered, . for blind.',
     '“Longest run of seconds with no repeated camera ID. That is our walk.”'],
    'Write longest_unique_substring(s) in one pass.',
    'Grow the window from the right. When a character repeats, shrink from the left until it does not.'),
  g('prefix','D',90,'memory','Running totals','Prefix sums',
    ['Cash flows in and out of the branch every hour. You will be asked, many times, how much moved between hour i and hour j.',
     '“Answer each question instantly. Do the work once.”'],
    'Write class RangeSum(nums) that precomputes prefix sums once, with a method sum_range(i, j) returning the sum of nums[i..j] inclusive in O(1).',
    'sum(i..j) is total(0..j) minus total(0..i-1). Precompute the totals.'),
  g('minsub','D',90,'speed','Enough cash in the window','Sliding window over sums',
    ['Hourly cash deliveries to the branch, all positive. The buyer wants at least a certain amount on the floor when the crew walks in.',
     '“Shortest stretch of consecutive hours whose deliveries reach the target. If no stretch does, say zero. One pass.”'],
    'Write min_subarray_len(target, nums) returning the length of the shortest contiguous run with sum at least target, or 0 if none. Stretch: why does the window trick break if deliveries can be negative?',
    'Extend the right edge until the sum reaches the target, then pull the left edge in while it still does. Record the shortest window each time it qualifies.',
`min_subarray_len(7, [2, 3, 1, 2, 4, 3])   # 2  (the 4 and the 3)
min_subarray_len(11, [1, 1, 1, 1])        # 0`),
  g('kadane','D',100,'logic','The best week','Kadane\'s algorithm',
    ['Daily deposits, some negative. The buyer wants the single contiguous stretch of days with the highest total.',
     '“Find the stretch. In one pass, or I hand this to Dax.”'],
    'Write max_subarray(nums) returning the maximum sum of a contiguous subarray.',
    'At each day, either extend the current run or start fresh here. Keep the best you have ever seen.'),
  g('product','D',100,'memory','Everyone\'s cut but yours','Prefix and suffix products',
    ['Five launderers in a chain, each taking a multiplier on what passes through. Any one of them might be a plant.',
     '“For each launderer, tell me what a bundle is worth going through everyone except them. No division, one of the multipliers is zero and I am not explaining that to the buyer.”'],
    'Write product_except_self(nums) returning a list where position i holds the product of every element except nums[i]. No division, O(n). Stretch: use O(1) extra memory beyond the output list.',
    'Running products from the left give everything before i. Running products from the right give everything after. Multiply the two; the running-totals gate had the same shape.',
`product_except_self([1, 2, 3, 4])    # [24, 12, 8, 6]
product_except_self([2, 0, 3])       # [0, 6, 0]`),
  g('twoptr','D',100,'speed','The getaway','Two pointers on sorted data',
    ['The van is idling. You have a sorted list of intersection distances and need two that are exactly the fuel range apart.',
     '“Four minutes. No nested loops.”'],
    'Write pair_with_diff(nums, d) for a sorted list, returning [a, b] with b - a == d, or None if no such pair exists. Then write three_sum(nums) returning every unique triplet that sums to zero, using the same idea.',
    'Start at both ends. If the pair is too big, move the right one; too small, move the left.'),
  g('linked','D',100,'memory','The patrol loop','Linked lists & cycle detection',
    ['The guards\' route is a chain of checkpoints, each pointing to the next. Marguerite suspects it loops.',
     '“Prove it loops without writing down every checkpoint you visit.”'],
    'Nodes are ListNode objects with val and next. Write reverse_list(head) returning the new head, and has_cycle(head) returning True or False in O(1) extra memory.',
    'Two runners on the track, one twice as fast. If there is a loop, the fast one laps the slow one.'),
  g('rotsearch','D',100,'logic','The rack was moved','Binary search on rotated data',
    ['Maintenance rehung the Halden key rack. Still sorted, still no gaps, but they started hanging from the middle: the smallest key is now somewhere along the wall and the numbers wrap around.',
     'Dax: “So we walk it again.”',
     '“We do not walk it again. It is still sorted in two pieces. Find the key in log n reads.”'],
    'Write search_rotated(nums, target) for a sorted list of distinct values rotated at an unknown pivot. Return the index or -1 in O(log n). Stretch: why does the same approach fail once duplicates are allowed?',
    'Cut the range in half. One half is always properly sorted; check whether the target belongs in that half and keep it or discard it accordingly.',
`search_rotated([4, 5, 6, 7, 0, 1, 2], 0)   # 4
search_rotated([4, 5, 6, 7, 0, 1, 2], 3)   # -1`),
  g('mergell','D',100,'speed','Two chains, one route','Linked list merging & the slow-fast pointer',
    ['Dax and Marguerite each recorded a chain of checkpoints by time, as linked lists this time. No indexes, only next pointers.',
     '“One chain, still in order. Reuse the nodes, we are not buying more memory. Then tell me which checkpoint sits in the middle without counting them first.”'],
    'Write merge_lists(a, b) merging two sorted singly linked lists into one sorted list, reusing the existing nodes. Stretch: write middle_node(head) returning the middle node in one pass.',
    'Start from a dummy head. Compare the two front nodes, attach the smaller, advance that list. For the middle: one pointer moving one step, another moving two.',
`class ListNode:
    def __init__(self, val, next=None):
        self.val, self.next = val, next

def from_list(xs):
    head = None
    for x in reversed(xs):
        head = ListNode(x, head)
    return head

def to_list(node):
    out = []
    while node:
        out.append(node.val); node = node.next
    return out

to_list(merge_lists(from_list([1, 2, 4]), from_list([1, 3, 4])))   # [1, 1, 2, 3, 4, 4]
middle_node(from_list([1, 2, 3, 4, 5])).val                        # 3`),
  g('tree','C',120,'logic','The org chart','Binary trees & traversal',
    ['Who reports to whom at the bank is a tree. You need to read it in three different orders and know how deep it goes.',
     '“Pre, in, post. And the height. Recursion is allowed, panic is not.”'],
    'Nodes are TreeNode objects with val, left and right. Write preorder(root), inorder(root) and postorder(root) returning lists of values, max_depth(root) returning the number of levels, and level_order(root) returning a list of levels, each a list of values.',
    'A tree is a node with two smaller trees. Whatever you do to the whole, do to the halves.'),
  g('bst','C',120,'logic','The sorted vault','Binary search trees',
    ['Safety deposit boxes are stored in a binary search tree by box number.',
     '“Insert. Find. Validate that nobody corrupted the ordering. Then give me the k-th smallest box.”'],
    'Nodes are TreeNode objects with val, left and right. Write insert(root, val) returning the root (root may be None), search(root, val) returning True or False, is_valid_bst(root), and kth_smallest(root, k) with k counted from 1.',
    'Inorder traversal of a BST visits keys in sorted order. Almost everything falls out of that fact.'),
  g('lca','C',140,'logic','The common handler','Lowest common ancestor',
    ['Two tellers on the org chart. Both have been paid. Marguerite wants the lowest manager who oversees both, because that manager signed off.',
     '“Lowest one. Not the CEO, everyone reports to the CEO. Then do it faster on the deposit-box tree, where the ordering tells you which way to go.”'],
    'Write lowest_common_ancestor(root, p, q) for a binary tree, returning the deepest node that has both p and q beneath it (a node counts as its own ancestor; p and q are nodes). Then write lca_bst(root, p, q), the O(h) version for a binary search tree. Stretch: write is_symmetric(root), which uses the same shape of recursion on two subtrees at once.',
    'Ask each subtree whether it found p or q. If both sides report a find, this node is the answer; if only one does, pass that result up. In a BST, compare the values to pick a side.'),
]},
{ name:'Arc III — The job', sub:'Underground. Three nights. No signal.', gates:[
  g('sewers','C',140,'logic','The sewer map','Breadth-first search on a grid',
    ['Under the financial district there is a grid of tunnels. Some are flooded. Dax is stuck at one junction and the extraction point is another.',
     '“Fewest turns. And tell me if there is no route at all before he wades in.”'],
    'Write shortest_path(grid, start, exit) where grid holds 0 for open and 1 for flooded, start and exit are (row, col) pairs, and moves are up, down, left, right. Return the number of steps, or -1 if no route exists.',
    'A queue explores the map in rings. The first time you reach the exit, that distance is already the shortest.'),
  g('islands','C',140,'speed','Vault compartments','Depth-first search & flood fill',
    ['The vault floor plan is a grid of steel and air. Connected air cells form a compartment.',
     '“How many compartments? And how big is the largest?”'],
    'Write count_regions(grid) and largest_region(grid) for a grid of 1 (air) and 0 (steel). Cells connect up, down, left and right; largest_region returns the cell count of the biggest compartment, 0 if there is no air.',
    'When you find an unvisited open cell, sink the whole compartment recursively before you move on.'),
  g('oranges','C',140,'logic','The gas spreads','Multi-source breadth-first search',
    ['Night two. The bank vents tear gas into the tunnels from several grates at once. Each minute it spreads to every open cell touching a gassed one.',
     'Dax: “Run a search from each grate, take the smallest time for every cell.”',
     '“Or start them all at once. How many minutes until the whole tunnel is gassed, and is there a cell it never reaches? That is where I am standing.”'],
    'Write minutes_to_fill(grid) for a grid of 0 (rock), 1 (open tunnel), 2 (gas source). Gas spreads to 4-adjacent open cells once per minute. Return the minutes until every open cell is gassed, or -1 if some cell is never reached. Stretch: return the coordinates of the last cell reached.',
    'Load every source into the queue before you start, all at distance zero. Then breadth-first search as usual; each layer is one minute.',
`grid = [
  [2, 1, 1],
  [1, 1, 0],
  [0, 1, 1],
]
minutes_to_fill(grid)   # 4

minutes_to_fill([[2, 1, 1], [0, 1, 1], [1, 0, 1]])   # -1  (bottom-left is cut off)`),
  g('clone','B',160,'memory','Copy the wiring','Graph deep copy with a visited map',
    ['The alarm wiring is a mesh: junction boxes, each listing the boxes it connects to. Marguerite wants to test cuts on a copy before touching the real one.',
     '“An exact copy. Same shape, new boxes. If two wires lead to the same box, they must lead to the same copy of it, or your test lies to us.”'],
    'Write clone_graph(node) returning a deep copy of a connected undirected graph. Each Node has val and a list neighbors. Every node must be copied exactly once. Stretch: write it iteratively with a queue.',
    'A dictionary from original node to its copy. When you reach a node already in the dictionary, reuse its copy instead of making another; that is what preserves the shape.',
`class Node:
    def __init__(self, val, neighbors=None):
        self.val = val
        self.neighbors = neighbors if neighbors is not None else []

a, b, c = Node(1), Node(2), Node(3)
a.neighbors = [b, c]; b.neighbors = [a, c]; c.neighbors = [a, b]

copy = clone_graph(a)
print(copy.val, [n.val for n in copy.neighbors])   # 1 [2, 3]
print(copy is a, copy.neighbors[0] is b)           # False False`),
  g('topo','B',160,'logic','Order of operations','Topological sort',
    ['Cut power before the cameras, cameras before the lock, lock before the door. Forty tasks, dozens of dependencies.',
     '“Give me an order that breaks nothing. Tell me if the plan contradicts itself.”'],
    'Write task_order(n, edges) for tasks 0..n-1 and edges (a, b) meaning a must happen before b. Return a list of every task in a valid order, or [] if the plan contradicts itself.',
    'Tasks with no remaining prerequisites can go now. Doing one may free others. Keep a queue of the free ones.'),
  g('timetable','B',160,'speed','The guard timetable','Sorting & greedy intervals',
    ['Every guard shift is an interval. You need the longest gap in the night with nobody on the floor.',
     '“Shifts overlap in ways that make no sense. Merge them first, then find the hole.”'],
    'Write merge_intervals(intervals) returning the merged shifts sorted by start (touching shifts merge too), and largest_gap(intervals) returning the longest stretch between merged shifts, or 0 if there is none.',
    'Sort by start. After that, each interval only needs comparing with the last one you kept.'),
  g('letters','B',160,'logic','The keypad','Recursive enumeration',
    ['Dax watched the branch manager punch a code into an old phone-style keypad: 2 is ABC, 3 is DEF, and so on up to 9. He saw the digits. He did not see the letters.',
     '“Every word those digits could spell. All of them, I will do the guessing. And do not write nine nested loops, the code might be ten digits tomorrow.”'],
    'Write letter_combinations(digits) returning every string the digit sequence could spell on a phone keypad, for digits 2–9. Empty input returns an empty list. Stretch: count the combinations without generating them.',
    'One digit at a time. For every partial word you have so far, extend it with each letter of the next digit. Recursion does this naturally; a growing list does it too.'),
  g('heap','B',160,'memory','What fits in the bag','Heaps & top-k',
    ['Ten thousand items in the vault, each with a value. The bag holds twenty.',
     '“The twenty most valuable. Then keep that answer current as Dax keeps finding things.”'],
    'Write k_largest(nums, k) returning the k largest values in descending order using a heap. Then write class TopK(k) with add(value) and top(), where top() returns the k largest values seen so far in descending order (fewer if fewer have been added).',
    'A min-heap of size k: if a new item beats the smallest thing in it, swap them. Python has heapq.'),
  g('counting','B',160,'speed','Badges by clearance','Counting sort & bucket sort',
    ['A hundred thousand staff badges, each with a clearance level from 0 to 9. Marguerite wants them sorted by level, and the badge order within a level must not change.',
     '“The keys are tiny and there are a lot of them. Comparing badges to each other is a waste. Count.”'],
    'Write counting_sort(nums, max_value) that sorts non-negative integers in O(n + k) and is stable. Then write bucket_sort(values) for floats in [0, 1). Stretch: why can neither replace a comparison sort for arbitrary integers?',
    'Count how many of each value there are. Running totals of the counts tell you where each value\'s block starts in the output; place items in original order into their block.'),
  g('meetings','B',160,'memory','Bodies on the floor','Heaps for overlapping intervals',
    ['The guard timetable again, but this time the question is not the gap. It is the crowd.',
     '“At the worst moment of the night, how many guards are on the floor at once? That is how many bodies we walk past. Sort the shifts, then keep the ones still running.”'],
    'Write max_concurrent(intervals) returning the maximum number of intervals overlapping at any moment (a shift ending at t and another starting at t do not overlap). Stretch: solve it again by sweeping sorted start and end times separately.',
    'Sort by start. A min-heap holds the end times of shifts still in progress; before adding a shift, pop every end time that is at or before its start. The heap\'s largest size is the answer.',
`max_concurrent([[0, 30], [5, 10], [15, 20]])   # 2
max_concurrent([[7, 10], [2, 4]])              # 1`),
  g('sorts','B',180,'logic','Sorting the take','Merge sort & quicksort',
    ['Every serial number, unsorted, and the buyer wants them ordered. sorted() is not available because you need to explain how it works to a jury.',
     '“Two ways. One that never has a bad night, one that is usually faster.”'],
    'Write merge_sort(nums) and quick_sort(nums), each returning a new sorted list without calling sorted() or .sort(). Compare their worst cases and explain when Python\'s built-in sort uses which idea.',
    'Merge sort: split, sort halves, merge. Quicksort: pick a pivot, partition around it, recurse. Where does each spend its time?'),
  g('quickselect','B',180,'speed','The twentieth most valuable','Quickselect',
    ['Ten thousand items, and Dax only wants to know one thing: the value of the item that would be twentieth if the vault were sorted. Not the top twenty, just the line.',
     '“You know where the partition step puts the pivot. Use that and only chase the side that matters. No heap, no full sort.”'],
    'Write kth_largest(nums, k) returning the k-th largest value in average O(n) time using quickselect. Stretch: what is the worst case, and why does choosing a random pivot make it rare?',
    'Partition around a pivot exactly as in quicksort. The pivot lands at its final sorted position; if that is position k you are done, otherwise recurse only into the side containing k.'),
  g('perms','B',180,'logic','Every route through the floor','Recursion & permutations',
    ['Five rooms, any order. Marguerite wants every possible order so she can rule them out one by one.',
     '“All of them. Then only the ones that start in the lobby.”'],
    'Write permutations(items) returning every ordering as a list of lists, recursively, then subsets(items) returning the power set as a list of lists. No itertools.',
    'Choose the first element, then permute the rest. To get subsets: for each item, either take it or leave it.'),
  g('safe','A',200,'logic','The safe combination','Backtracking',
    ['The inner safe wants a 6-digit code where no digit repeats and each adjacent pair sums to a prime.',
     '“Enumerate every valid code. There are fewer than you think.”'],
    'Write safe_codes() returning every 6-character digit string (leading zero allowed) with no repeated digit where each adjacent pair of digits sums to a prime, pruning dead branches early. Then write n_queens(n) returning the number of ways to place n queens on an n×n board, with the same skeleton.',
    'Build one digit at a time. If the rule fails now, nothing you add later can fix it. Undo and try the next digit.'),
  g('task','A',220,'speed','The crew\'s rota','Greedy scheduling with a heap',
    ['Night three has a list of jobs by type: drill, drill, listen, drill, cut. Two jobs of the same type need n quiet slots between them or the sensors notice the rhythm.',
     '“Fewest total slots, idle ones included. Always do whatever you have the most of left, because that is what will strand you at the end.”'],
    'Write least_interval(tasks, n) where tasks is a list of single-character job types and n is the required cooldown between two of the same type. Return the minimum number of time slots, counting idle slots. Stretch: derive the closed-form answer from the most frequent type alone.',
    'A max-heap of remaining counts. Each round, take up to n+1 different types, run them, put the survivors back. Rounds that come up short are padded with idle slots.',
`least_interval(["A", "A", "A", "B", "B", "B"], 2)   # 8   A B _ A B _ A B
least_interval(["A", "A", "A", "B", "B", "B"], 0)   # 6`),
  g('wordsearch','A',240,'logic','The word in the grid','Backtracking on a grid',
    ['The inner door has no keypad. It has a grid of letters, and the passphrase must be traced with a finger through touching cells, never stepping on a cell twice.',
     'Dax wants to try every path. Marguerite wants him to stop talking.',
     '“Tell me whether the word is in there before he wears the finish off. Start anywhere. Stop the moment a letter fails to match.”'],
    'Write exist(board, word) returning True if word can be traced through horizontally or vertically adjacent cells with no cell reused. Stretch: given a list of many words, what structure would let you search for all of them in one traversal?',
    'From each cell matching the first letter, recurse on the neighbours for the next letter. Mark the cell as used on the way in and unmark it on the way out.',
`board = [
  ["A", "B", "C", "E"],
  ["S", "F", "C", "S"],
  ["A", "D", "E", "E"],
]
exist(board, "ABCCED")   # True
exist(board, "SEE")      # True
exist(board, "ABCB")     # False`),
]},
{ name:'Arc IV — The escape', sub:'The city is a graph and every edge has a price.', gates:[
  g('bipartite','A',200,'logic','Two safehouses','Graph two-colouring',
    ['Out of the bank, into the city. The crew and their informants have to split between two safehouses tonight, and Marguerite has a list of pairs who must never be under one roof.',
     '“Two houses. Every pair on this list in different ones. Tell me if that is even possible before I start handing out addresses.”'],
    'Write is_bipartite(graph) where graph[i] lists the nodes that must not share a house with i. Return True if the nodes can be split into two groups with no edge inside a group. Stretch: return the two groups.',
    'Give a node a colour, then every neighbour the opposite colour, breadth-first. If you ever have to paint a node the colour it already is not, the split is impossible.'),
  g('routes','A',220,'speed','Every road out of the city','Dijkstra\'s algorithm',
    ['Police cordons have prices now: seconds of delay per road. You have the whole map with weights.',
     '“Cheapest route to any of three safehouses. Recalculate if a road closes.”'],
    'Compute the minimum-cost route from the bank to the nearest safehouse over a weighted graph.',
    'A priority queue always hands you the closest unsettled node. Once settled, a node never gets cheaper.'),
  g('union','A',220,'memory','Who talks to whom','Union-find',
    ['Every informant who has ever spoken to another. Thousands of pairs.',
     '“How many separate networks? And when a new pair comes in, tell me instantly if they were already connected.”'],
    'Implement union-find with path compression and use it to count connected components.',
    'Each node points to a parent. Find the root. Two nodes are connected if they share a root.'),
  g('mst','A',220,'speed','Wiring the safehouses','Minimum spanning tree (Kruskal)',
    ['Seven safehouses need secure lines between them so any one can reach any other, directly or through the rest. The fence quoted a price per pair.',
     'Dax: “Pay for all of them, then we have backups.”',
     '“We have no budget for backups. Cheapest set of lines that still connects everyone. You built the tool for this last gate.”'],
    'Write min_wiring_cost(n, edges) where edges are (u, v, cost) triples over nodes 0..n-1. Return the total cost of the cheapest set of edges connecting all nodes, or -1 if they cannot all be connected. Stretch: solve it again with Prim\'s algorithm and a heap, and say when each wins.',
    'Sort the edges by cost. Take each edge whose ends are in different components (union-find tells you in near-constant time) and merge them. Stop after n-1 edges.',
`edges = [(0, 1, 4), (0, 2, 3), (1, 2, 1), (1, 3, 2), (2, 3, 4), (3, 4, 2), (4, 5, 6)]
min_wiring_cost(6, edges)   # 14
min_wiring_cost(3, [(0, 1, 5)])   # -1`),
  g('bellman','A',220,'memory','Roads that pay you','Bellman-Ford',
    ['Some cordons are staffed by cops on the payroll. Driving through those saves time: the road has a negative cost. The priority-queue route finder now gives wrong answers.',
     '“Recompute with negatives allowed. And if some loop of roads pays you forever, tell me, because that means the map is lying.”'],
    'Write bellman_ford(n, edges, src) for directed (u, v, w) edges where w may be negative. Return the list of shortest distances from src, or None if a negative cycle is reachable. Stretch: explain why n-1 rounds of relaxation are always enough when there is no negative cycle.',
    'Relax every edge, and repeat that n-1 times: a shortest path uses at most n-1 edges. If an n-th round still improves anything, a negative loop is feeding it.',
`edges = [(0, 1, 4), (0, 2, 5), (1, 2, -3), (2, 3, 4), (3, 1, -2)]
bellman_ford(4, edges, 0)   # None  (1 -> 2 -> 3 -> 1 keeps paying)

edges = [(0, 1, 4), (0, 2, 5), (1, 2, -3), (2, 3, 4)]
bellman_ford(4, edges, 0)   # [0, 4, 1, 5]`),
  g('stairs','A',220,'logic','The fire escape','Dynamic programming, first steps',
    ['A fire escape with n steps. You can take one or two at a time. Marguerite wants to know how many distinct ways up there are.',
     '“Then do it for a hundred steps without waiting until morning.”'],
    'Count the ways to climb n stairs. First recursively, then with memoization, then bottom-up.',
    'Ways(n) = ways(n-1) + ways(n-2). The recursion is right but recomputes. Remember answers.'),
  g('robber','A',220,'memory','The row of stashes','Dynamic programming on a line',
    ['A street of stashes, each holding a known amount. Hit two neighbours on the same night and the network notices and empties the rest.',
     '“Most cash, no two adjacent. Same idea as the stairs, only now the steps are worth money.”'],
    'Write rob(nums) returning the maximum total from non-adjacent positions. Stretch: the stashes form a circle, so the first and last are also neighbours.',
    'Best up to i is either best up to i-1 (skip this one) or nums[i] plus best up to i-2 (take it). Two running variables are enough.'),
  g('jump','A',220,'speed','Rooftop hops','Greedy reach',
    ['Rooftops in a line. From each one you can leap at most a certain number of roofs forward; some roofs are dead ends with a zero.',
     'Dax: “Try every sequence of jumps.”',
     '“That is exponential and you are heavy. Walk the roofs once and keep track of the farthest one we can still reach.”'],
    'Write can_jump(nums) returning True if the last index is reachable from the first, where nums[i] is the maximum jump length from i. Stretch: write min_jumps(nums) returning the fewest jumps needed.',
    'Keep one number: the farthest index reachable so far. Walking forward, if you ever stand beyond it you are stuck; otherwise extend it with i + nums[i].'),
  g('paths','A',220,'logic','Crossing the grid','Dynamic programming on a grid',
    ['The financial district is a grid of one-way streets: you can only go south or east. Marguerite wants to know how many distinct routes reach the safehouse corner, because the police can only watch so many.',
     '“Count the routes. Then count them again with the blocked intersections I am about to send you.”'],
    'Write unique_paths(m, n) returning the number of paths from the top-left to the bottom-right of an m×n grid moving only right or down. Then write unique_paths_with_obstacles(grid) where 1 marks a blocked cell. Stretch: do it in O(n) memory.',
    'The ways to reach a cell equal the ways to reach the cell above plus the ways to reach the cell to the left. A blocked cell contributes zero.'),
  g('coins','A',240,'memory','Exact change for the border','Coin change',
    ['The border guard takes exact change only and you have coins of odd denominations.',
     '“Fewest coins that make the amount. Or tell me it cannot be done.”'],
    'Minimum coins to make a target amount, unbounded supply. Return -1 if impossible.',
    'Best(amount) = 1 + min over coins of best(amount - coin). Build the table from 0 up.'),
  g('minpath','A',240,'logic','The cheapest crossing','Grid dynamic programming with costs',
    ['Same one-way grid, but now every intersection has a price: seconds of camera exposure. Marguerite does not want the count of routes any more. She wants the one route she can afford.',
     '“Least total exposure from the corner we are on to the safehouse. Then show me the route itself, not just the number.”'],
    'Write min_path_sum(grid) returning the minimum sum of cell values along a path from top-left to bottom-right moving only right or down. Stretch: reconstruct the path as a list of coordinates.',
    'Same table as the route count, but each cell holds its own cost plus the cheaper of the cell above and the cell to the left.'),
  g('decode','A',240,'memory','The numbered message','Dynamic programming on a string',
    ['The fence sends a string of digits. Her code maps 1 to A through 26 to Z, but she never puts in separators. 226 could be BZ, VF, or BBF.',
     '“How many readings does this message have? I need to know how badly she is messing with me.”'],
    'Write num_decodings(s) returning the number of ways to decode a digit string with 1..26 mapping to A..Z. Strings with a leading or unattached zero decode zero ways. Stretch: what is the answer for "0" and for "10", and why?',
    'Ways up to position i: add ways up to i-1 if the single digit is not 0, and add ways up to i-2 if the last two digits form 10..26.'),
  g('lis','A',240,'speed','The rising tide','Longest increasing subsequence',
    ['Bribe amounts over the years. The prosecutor wants the longest run that only ever goes up, not necessarily consecutive.',
     '“O(n²) first. Then make it n log n or I find someone who can.”'],
    'Find the length of the longest strictly increasing subsequence, first in O(n²) DP, then with binary search.',
    'Keep a list of the smallest tail for each subsequence length. Binary search to place each new number.'),
  g('wordbreak','A',240,'memory','Where the words split','Dynamic programming over prefixes',
    ['An intercepted note, all the spaces stripped out. The crew has a dictionary of the code words the other side uses.',
     '“Can this string be cut into dictionary words, nothing left over? Do not try every cut; remember which prefixes you already know can be cut.”'],
    'Write word_break(s, word_dict) returning True if s can be segmented into a sequence of dictionary words (reuse allowed). Stretch: return every valid segmentation.',
    'Can the first i characters be broken? Yes if some earlier j can be broken and s[j:i] is a word. Fill from the empty prefix, which trivially can.',
`word_break("vaultdoor", ["vault", "door"])                    # True
word_break("cashvancash", ["cash", "van"])                    # True
word_break("guardsandog", ["guards", "dog", "sand", "and", "guard"])   # False`),
]},
{ name:'Arc V — The Ledger', sub:'The job was never the money.', gates:[
  g('knap','S',280,'logic','The bag, properly this time','0/1 knapsack',
    ['Weight limit, twenty items, each with weight and value. Take it or leave it, no halves.',
     '“Maximum value under the limit. Brute force is two to the twenty. I know you can do better.”'],
    'Solve 0/1 knapsack with a DP table, then reduce it to a single row.',
    'Best(i, w) either skips item i or takes it: best(i-1, w) vs value[i] + best(i-1, w - weight[i]).'),
  g('rain','S',280,'speed','Water in the tunnel','Two pointers with running maxima',
    ['The tunnel floor is uneven, a list of heights. Rain is coming and the low stretches will flood. Marguerite wants to know how much water the tunnel will hold before it drains.',
     '“Total volume. Water sits on a cell only as high as the lower of the tallest walls to either side of it. One pass, no scratch arrays.”'],
    'Write trap(height) returning the total units of water held between the bars. O(n) time and O(1) extra memory. Stretch: first solve it with two arrays of running maxima, then argue why the two-pointer version can drop them.',
    'Water above a cell is min(highest to the left, highest to the right) minus its height. Move the pointer on the lower side; that side\'s maximum is the one that binds.'),
  g('edit','S',280,'memory','The forged signature','Edit distance',
    ['The signature on the transfer differs from the real one by a few strokes. The bank\'s software allows three.',
     '“How many single-character edits between these two strings? Insert, delete, replace.”'],
    'Compute the Levenshtein distance between two strings.',
    'A 2-D table: distance between prefixes. Each cell is 1 + the min of its three neighbours, or free if the characters match.'),
  g('deque','S',280,'speed','The camera sweep','Monotonic deque',
    ['Motion-sensor readings from the lobby camera, one per second. Security only reacts to the peak reading in any k-second span.',
     'Dax: “For each span, look at all k numbers.”',
     '“That is n times k and the night is short. Every reading gets looked at twice at most.”'],
    'Write max_sliding_window(nums, k) returning the maximum of every window of k consecutive values, in O(n). Stretch: explain why a heap gives O(n log k) and where it wastes work.',
    'Keep a deque of indices whose values are decreasing. Pop the front when it falls out of the window; pop the back while its value is smaller than the new one. The front is always the max.'),
  g('lcs','S',280,'logic','What the two copies share','Longest common subsequence',
    ['Two copies of the same ledger page, both edited by hand. The buyer trusts only the entries that appear in both, in the same order, gaps allowed.',
     '“Longest run of entries common to both copies. You already built the distance table for the signature; this is its cousin.”'],
    'Write lcs(a, b) returning the length of the longest common subsequence of two strings. Stretch: reconstruct the subsequence itself by walking the table backwards.',
    'If the last characters match, it is one plus the answer for both prefixes shortened. If not, the better of shortening either one.'),
  g('lru','S',300,'memory','The fence\'s memory','LRU cache',
    ['The fence keeps a mental list of contacts. Her list holds only so many. Use a name and it moves to the front; add a name when the list is full and the one she has not used longest is forgotten.',
     '“Build me her memory. get and put, both in constant time. If I have to wait for you, she has already forgotten me.”'],
    'Write class LRUCache(capacity) with get(key) returning the value or -1, and put(key, value). Both O(1); when full, put evicts the least recently used key. Stretch: change the policy to least frequently used.',
    'A dictionary for O(1) lookup, a doubly linked list for O(1) reordering; the dictionary points into the list. Python\'s OrderedDict is a fair first draft before you build it by hand.',
`cache = LRUCache(2)
cache.put(1, 1)
cache.put(2, 2)
cache.get(1)      # 1
cache.put(3, 3)   # evicts key 2
cache.get(2)      # -1
cache.put(4, 4)   # evicts key 1
cache.get(1)      # -1
cache.get(3)      # 3
cache.get(4)      # 4`),
  g('trie','S',300,'speed','The informant\'s word list','Tries & prefix search',
    ['Two hundred thousand code words. Dax radios in fragments. You need every word that starts with what he said, instantly.',
     '“Autocomplete, but for people who might shoot us.”'],
    'Build a trie supporting insert, search, and starts_with; return all words under a prefix.',
    'One node per character, shared prefixes share a path. Walk the prefix, then collect everything beneath.'),
  g('kmp','S',300,'logic','The pattern in the wire','String matching (KMP)',
    ['A tapped line, hours of characters. Somewhere in it a code word repeats, and every time it does a payment moved.',
     'Dax: “Check every position. If it fails, slide one and start over.”',
     '“Starting over throws away what you already matched. When you mismatch, you already know what the last few characters were. Use that.”'],
    'Write find_all(text, pattern) returning every starting index where pattern occurs in text, in O(n + m). Build the prefix (failure) table first. Stretch: solve it again with a rolling hash (Rabin-Karp) and say what a collision costs you.',
    'The failure table says, for each prefix of the pattern, the length of its longest proper prefix that is also a suffix. On mismatch, fall back to that length instead of moving the text pointer.'),
  g('palsub','S',300,'memory','The mirrored account number','Expand around the centre',
    ['Shell company account numbers. The fence says the real one hides a mirrored stretch, the longest run that reads the same both ways.',
     '“Longest mirrored stretch, and give me the stretch, not the length. n squared is acceptable tonight. Faster is not required.”'],
    'Write longest_palindrome(s) returning the longest palindromic substring. Stretch: there is an O(n) algorithm for this; find its name and describe the trick in two sentences without implementing it.',
    'Every palindrome has a centre: a character, or the gap between two. Expand outwards from each centre while the ends match; keep the longest.'),
  g('ladder','S',320,'speed','One letter at a time','Breadth-first search over words',
    ['Code words change one letter per handoff, and each intermediate word must be a real word from the fence\'s list. Marguerite has the word they started with and the word they ended with.',
     '“Fewest handoffs from one to the other. Zero if it cannot be done. The list is big, so do not compare every word to every other word.”'],
    'Write ladder_length(begin, end, word_list) returning the number of words in the shortest chain from begin to end (inclusive), changing one letter at a time and passing only through words in word_list. Return 0 if impossible. Stretch: search from both ends at once.',
    'Words are nodes; two words are joined if they differ in one letter. Breadth-first search finds the shortest chain. Bucket words by patterns like h*t so neighbours are found without pairwise comparison.',
`ladder_length("hit", "cog", ["hot", "dot", "dog", "lot", "log", "cog"])   # 5
ladder_length("hit", "cog", ["hot", "dot", "dog", "lot", "log"])          # 0`),
  g('alien','S',320,'logic','The fence\'s alphabet','Topological sort from ordering constraints',
    ['A page of the Ledger is sorted, but not by any alphabet you know. The fence\'s people use their own letter order and never wrote it down.',
     '“Recover the alphabet from the sorted words. If the page contradicts itself, tell me, because then someone edited it.”'],
    'Write alien_order(words) returning a string of the letters in the unknown alphabet\'s order, given words sorted by that alphabet. Return "" if no consistent order exists. Stretch: when several orders fit, which one do you return, and how would you make it deterministic?',
    'Compare each adjacent pair of words: the first differing letter gives one edge, earlier before later. Then it is the order-of-operations gate again, over letters.',
`alien_order(["wrt", "wrf", "er", "ett", "rftt"])   # "wertf"
alien_order(["z", "x", "z"])                        # ""`),
  g('median','S',340,'speed','The middle of two ledgers','Binary search on a partition',
    ['Two ledgers, each sorted by amount. The prosecutor wants the median payment across both. Merging them would take all night and leave a copy.',
     '“Median of both together. Logarithmic. You may look at the shorter book as many times as you need, as long as you halve it each time.”'],
    'Write find_median_sorted_arrays(a, b) returning the median of two sorted lists in O(log(min(m, n))). Stretch: generalise to the k-th smallest element of the two combined.',
    'Cut the shorter list at some point; the cut in the longer one is then forced so the left halves together hold half the elements. Binary search the cut until everything left is at most everything right.'),
  g('regex','S',340,'logic','The wildcard clause','Dynamic programming over two strings',
    ['The Ledger\'s index uses patterns: a dot matches any single character, and a star means zero or more of the character before it. Marguerite wants to know which entries a pattern covers.',
     '“Does this entry match this pattern? Whole entry, not a piece of it. Recursion is fine until it is not; then remember what you already answered.”'],
    'Write is_match(s, p) returning True if the whole string s matches pattern p, where "." matches any single character and "*" matches zero or more of the preceding element. Stretch: solve the variant where "?" matches one character and "*" matches any sequence.',
    'A table over prefixes of s and p. A star can match zero of its character (skip two pattern characters) or one more (consume a string character if it matches and stay on the same pattern position).'),
  g('balloons','S',360,'memory','Collapsing the shell companies','Interval dynamic programming',
    ['A row of shell companies, each with a value. Collapse one and you collect the product of its two neighbours\' values times its own; then the row closes up and the neighbours change.',
     'Dax: “Collapse the biggest first.”',
     '“Wrong. Think about the one you collapse last in any stretch. Everything on either side of it was already gone, and never interfered.”'],
    'Write max_coins(nums) returning the maximum total collectable by collapsing every company, where collapsing index i yields nums[i-1] * nums[i] * nums[i+1] and missing neighbours count as 1. Stretch: matrix chain multiplication has the same shape; write it too.',
    'Pad the row with a 1 at each end. For each range, try each element as the last one collapsed in that range; the two sides become independent subproblems.'),
  g('ledger','S',400,'logic','The Ledger','Subset sum & reconstruction',
    ['The job was never the money. It was a book of payments, and the buyer will only take a subset whose values total exactly the amount they can launder.',
     '“Tell me which pages. Brute force is a lifetime.”',
     'Marguerite closes the book. “After this, you are not the systems person. You are the one who holds the Ledger.”'],
    'Given page values and a target total, decide whether an exact subset exists and reconstruct which pages make it.',
    'Can I make total t with the first k pages? Fill the table, then walk it backwards to recover the choices.'),
  g('epilogue','S',400,'speed','Epilogue: the couriers','Binary search on the answer + greedy check',
    ['Dawn. The Ledger sits on the table in its pages, each page weighted by how much trouble it carries. k couriers wait outside, and the pages must leave in order, each courier taking a consecutive run.',
     'Dax: “Try every way to split them.”',
     'Marguerite does not answer him. She looks at you.',
     '“Nobody carries more than they must. You have done this twice already tonight, in two different rooms. Put them together.”'],
    'Write split_array(nums, k) returning the smallest possible value of the largest sum among k non-empty contiguous pieces of nums. Stretch: name the two earlier gates you combined, and explain why the greedy check is allowed to be greedy.',
    'Guess a maximum load. Checking a guess is greedy: walk the pages, start a new courier whenever the next page would exceed it, and count couriers. Guesses that work form a suffix, so binary search the boundary.',
`split_array([7, 2, 5, 10, 8], 2)   # 18   [7, 2, 5] [10, 8]
split_array([1, 2, 3, 4, 5], 2)    # 9    [1, 2, 3] [4, 5]
split_array([1, 4, 4], 3)          # 4`),
]},
];

export const ARCS_DATA = ARCS;
